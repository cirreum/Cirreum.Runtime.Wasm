@* 
SessionActivityMonitor.razor - A headless component for monitoring DOM user activity
Drop this into App.razor to enable automatic session extension based on user interactions.
*@

@namespace Cirreum.Components.Authorization
@implements IAsyncDisposable

@* This component has no UI - it only manages JavaScript interop for activity monitoring *@